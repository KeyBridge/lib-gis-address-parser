<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <groupId>org.caulfield</groupId>
  <artifactId>address-parser</artifactId>
  <version>0.1</version>
  <packaging>jar</packaging>
  <name>Address Parser</name>
  <description>Utility class to parse and standardize address information.
    
Address standardization is the process that takes an address 
and converts it to a standard format by analyzing the several components. 
Standardization is not correction ! Standardization is based on syntax 
correction (dictionnaries, spellchecking, synonyms), while correction 
is based on postal reference data : correction checks if each element
exists and if the combination is correct.</description>
  <organization>
    <name>Key Bridge Global LLC</name>
    <url>http://keybridgeglobal.com</url>
  </organization>
  <url>http://keybridgeglobal.com</url>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.3.2</version>
        <configuration>
          <source>1.7</source>
          <target>1.7</target>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>wsif</artifactId>
      <version>6.0</version>
    </dependency>
  </dependencies>
  
  <distributionManagement>
    <repository>
      <id>archiva.internal</id>
      <name>Internal Release Repository</name>
      <url>http://black:8088/repository/internal/</url>
    </repository>
    <snapshotRepository>
      <id>archiva.snapshots</id>
      <name>Internal Snapshot Repository</name>
      <url>http://black:8088/repository/snapshots/</url>
    </snapshotRepository>
  </distributionManagement>

</project>
